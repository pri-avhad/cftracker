import React from 'react'
import './result.scss'

const Result = ({result}) => (
    <div className = "innerContainer">
    <h3>{result} kgs/month</h3>
    </div>
    //add button to update DB
);

export default Result;